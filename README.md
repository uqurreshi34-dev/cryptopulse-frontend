This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, run the development server:

```
✅ Project flow

Backend → Render, HTTPS, CORS locked, DB live

Frontend → Vercel, HTTPS, fetches backend correctly

Dynamic pages [symbol] → 404 works

Filters, sorting, slider → work, query params update URL

JSON API → safe, tested via Postman / Render logs
```
